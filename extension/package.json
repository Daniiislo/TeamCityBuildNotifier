{
  "name": "teamcity-build-notifier",
  "version": "1.0.0",
  "description": "TeamCity build notification extension",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/**/*.test.js"
  },
  "type": "module",
  "dependencies": {
    "@microsoft/signalr": "^8.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
